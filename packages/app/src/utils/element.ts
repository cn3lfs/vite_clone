import { ElMessageBox, ElMessage } from "element-plus";

export const MessageConfirm = ElMessageBox.confirm;
export const MessageSuccess = ElMessage.success;
export const MessageError = ElMessage.error;
export const MessageWarning = ElMessage.warning;
